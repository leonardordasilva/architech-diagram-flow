/** Centralised diagram-interaction constants.
 *  Import from here instead of declaring inline to keep values consistent. */

/** Pixel distance within which two nodes snap to the same axis during drag. */
export const SNAP_THRESHOLD = 8;

/** Maximum number of characters allowed in a diagram name. */
export const MAX_DIAGRAM_NAME_LENGTH = 100;

/** Character count at which the diagram-name counter warning becomes visible. */
export const DIAGRAM_NAME_WARNING_THRESHOLD = 80;
