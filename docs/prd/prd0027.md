PRD v3.0 — Resolução dos Itens Pendentes (Score 94 → 100)
Projeto: architech-diagram-flow (MicroFlow Architect) Data: 08/04/2026 Executor: Lovable AI Score atual: 94/100 Meta: 100/100 (Enterprise-ready) Premissas: Lovable Cloud (sem browser), Supabase permanente, sem CI/CD externo

Contexto
A auditoria R3.1 identificou 12 itens de melhoria. Após execução do PRD v2.0, 9 dos 12 foram implementados com sucesso. Restam exatamente 3 itens incompletos, cada um com escopo claro e arquivos-alvo específicos.

ITEM 1 — Migrar estilos inline restantes de PricingCards.tsx para CSS
Pilar: Arquitetura & Estrutura (19 → 20) Referência: F3-T2 (parcialmente implementado)

Problema
O arquivo src/components/PricingCards.css já foi criado com classes para botões (.p-btn-primary, .p-btn-blue, .p-btn-outline), toggle (.p-cycle-btn) e features (.p-feature-item). Porém, src/components/PricingCards.tsx ainda contém aproximadamente 40 blocos style={{…}} inline distribuídos nos seguintes elementos: container principal, bloco header, badge "Planos e Preços", título h2, subtítulo p, toggle container, toggle wrapper, grid de cards, cada card div (com variações condicionais para highlight e preselected), glow decorativo, header do card, icon wrapper, plan name, badge "Popular", price container, price value, per-note, divider, feature list ul, check icon wrapper, check icon e icon do Loader2.

O que fazer
Criar as seguintes classes CSS em src/components/PricingCards.css, usando as CSS custom properties já existentes (--pc-surface, --pc-border, --pc-text-muted, --pc-text-dim, --pc-blue-accent, --pc-blue-text):

.p-container — width 100%, max-width 1100px, margin 0 auto. .p-header — text-align center, margin-bottom 48px, animation fadeInUp 0.6s ease both. .p-badge — display inline-flex, align-items center, gap 6px, background rgba(59,130,246,0.1), border 1px solid rgba(59,130,246,0.25), border-radius 100px, padding 5px 14px, font-size 12px, font-weight 600, color #93c5fd, margin-bottom 24px, letter-spacing 0.04em, text-transform uppercase. .p-title — font-family 'Space Grotesk' sans-serif, font-size clamp(28px, 4vw, 44px), font-weight 700, line-height 1.15, margin 0 0 16px, color #f1f5f9. .p-subtitle — font-size 18px, color #94a3b8, max-width 500px, margin 0 auto, line-height 1.6. .p-toggle-wrap — display flex, justify-content center, margin-bottom 48px. .p-toggle-group — display flex, gap 4px, background rgba(255,255,255,0.04), border 1px solid rgba(255,255,255,0.1), border-radius 12px, padding 5px. .p-grid — display grid, grid-template-columns repeat(auto-fit, minmax(300px, 1fr)), gap 24px, align-items stretch. .p-card-base — backdrop-filter blur(10px), -webkit-backdrop-filter blur(10px), border-radius 20px, padding 32px, display flex, flex-direction column, gap 0, position relative, overflow hidden. .p-card--default — background rgba(255,255,255,0.03), border 1px solid rgba(255,255,255,0.08), box-shadow 0 4px 24px rgba(0,0,0,0.2). .p-card--highlight — background linear-gradient(145deg, rgba(59,130,246,0.08), rgba(139,92,246,0.05)), border 1px solid rgba(59,130,246,0.35), box-shadow 0 0 0 1px rgba(59,130,246,0.2) e 0 20px 60px rgba(59,130,246,0.12). .p-card--preselected — background rgba(245,158,11,0.05), border 1px solid rgba(245,158,11,0.5), box-shadow 0 0 0 1px rgba(245,158,11,0.25) e 0 16px 48px rgba(245,158,11,0.12). .p-glow — position absolute, top -40px, right -40px, width 200px, height 200px, border-radius 50%, background radial-gradient(circle, rgba(59,130,246,0.15) 0%, transparent 70%), pointer-events none. .p-plan-header — display flex, align-items center, justify-content space-between, margin-bottom 20px. .p-plan-header-left — display flex, align-items center, gap 10px. .p-icon-box — width 38px, height 38px, border-radius 10px, display flex, align-items center, justify-content center. .p-icon-box--blue — background rgba(59,130,246,0.15). .p-icon-box--amber — background rgba(245,158,11,0.15). .p-icon-box--gray — background rgba(107,114,128,0.15). .p-plan-name — font-family 'Space Grotesk' sans-serif, font-weight 700, font-size 18px, color #f1f5f9. .p-popular-badge — background linear-gradient(135deg, #3b82f6, #8b5cf6), color #fff, font-size 11px, font-weight 700, padding 3px 10px, border-radius 100px, letter-spacing 0.04em, text-transform uppercase. .p-price-section — margin-bottom 28px. .p-price-row — display flex, align-items baseline, gap 4px. .p-price-value — font-family 'Space Grotesk' sans-serif, font-size 44px, font-weight 800, line-height 1, color #f1f5f9. .p-price-value--highlight — color #93c5fd. .p-per-note — font-size 13px, color #64748b, margin-top 4px. .p-divider — height 1px, background rgba(255,255,255,0.07), margin-bottom 24px. .p-feature-list — list-style none, padding 0, margin 0 0 32px, display flex, flex-direction column, gap 12px, flex 1. .p-check-circle — width 18px, height 18px, border-radius 50%, background rgba(34,197,94,0.15), display flex, align-items center, justify-content center, flex-shrink 0, margin-top 1px. .p-icon-sm — width 18px, height 18px. .p-icon-xs — width 12px, height 12px. .p-icon-check — width 10px, height 10px, color #4ade80. .p-icon-loader — width 16px, height 16px, animation spin 1s linear infinite.

Depois, em PricingCards.tsx, substituir cada bloco style={{…}} pela classe CSS correspondente. Para os cards, combinar .p-card p-card-base com o modificador condicional (p-card--highlight, p-card--preselected, ou p-card--default). Para o animationDelay dinâmico por card, manter apenas style={{ animationDelay: '${idx * 0.1}s' }} — este é o único style inline permitido pois depende do índice.

Arquivos a editar
src/components/PricingCards.css — adicionar as novas classes. src/components/PricingCards.tsx — substituir style={{…}} por className.

Critério de aceite
Máximo 3 blocos style={{…}} remanescentes (apenas animationDelay por card, 1 por card = 3 total para 3 plans). O teste existente src/components/PricingCards.test.tsx deve continuar passando sem alterações. Renderização visual idêntica — mesmas cores, tamanhos, espaçamentos.

ITEM 2 — Surfacear integrityWarning no carregamento inicial e na listagem de diagramas
Pilar: Banco & Escalabilidade (19 → 20) Referência: F4-T1 (parcialmente implementado)

Problema
O campo integrityWarning é propagado corretamente no DiagramRecord por toDiagramRecord() e um toast é exibido quando o usuário clica "Refresh" (via useDiagramRefresh.ts). Porém, quando o usuário abre um diagrama pela primeira vez clicando nele em "Meus Diagramas" (MyDiagrams.tsx → handleLoad), o integrityWarning não é verificado nem exibido. Além disso, na listagem de diagramas, não existe indicação visual de que um diagrama tem problema de integridade.

O que fazer
Parte A — Toast ao carregar diagrama pela lista:

Em src/pages/MyDiagrams.tsx, na função handleLoad(diagram: DiagramRecord), logo após carregar o diagrama no store, adicionar uma verificação:

Copyif (diagram.integrityWarning) {
  toast({
    title: t('integrity.warning'),
    description: t('integrity.hashMismatch'),
    variant: 'destructive',
    duration: 10000,
  });
}
Parte B — Ícone de warning na listagem:

Na renderização do grid de cards de diagrams.map(...), dentro de cada card, logo após o título <h3>, adicionar condicionalmente:

Copy{d.integrityWarning && (
  <Tooltip>
    <TooltipTrigger asChild>
      <AlertTriangle className="h-4 w-4 text-amber-500 shrink-0" />
    </TooltipTrigger>
    <TooltipContent>{t('integrity.hashMismatch')}</TooltipContent>
  </Tooltip>
)}
O ícone AlertTriangle já está importado em MyDiagrams.tsx. O TooltipProvider já envolve os botões de ação. Mover o <TooltipProvider> para envolver o card inteiro (ou o título + ícone) para que o tooltip funcione.

Parte C — Chaves i18n:

As chaves integrity.warning e integrity.hashMismatch já existem em src/i18n/locales/en.json. Verificar se existem também em pt.json e adicioná-las caso estejam ausentes:

Copy"integrity": {
  "warning": "Aviso de integridade",
  "hashMismatch": "O hash de integridade deste diagrama não confere. Os dados podem ter sido modificados fora do fluxo normal."
}
Arquivos a editar
src/pages/MyDiagrams.tsx — adicionar toast no handleLoad e ícone no card. src/i18n/locales/pt.json — adicionar chaves integrity.* se ausentes.

Critério de aceite
Ao carregar um diagrama com integrityWarning preenchido, um toast destrutivo de 10 segundos é exibido. Na listagem, diagramas com integrityWarning mostram um ícone de triângulo amarelo com tooltip. Testes existentes continuam passando.

ITEM 3 — Criar testes unitários shareService.test.ts e workspaceService.test.ts
Pilar: Preparação para Crescimento (18.5 → 20) Referência: F5-T2 (não implementado)

Problema
Os handlers MSW para os endpoints de share e workspace já foram adicionados ao arquivo de mock handlers (cobrem diagram_shares, profiles, search_users_by_email, share-diagram, get_user_workspace, get_workspace_members, feature_flags). Porém, nenhum arquivo de teste foi criado para validar as funções exportadas de shareService.ts e workspaceService.ts.

O que fazer
Arquivo src/services/shareService.test.ts:

Seguir exatamente o padrão de src/services/diagramService.test.ts: importar setupServer do MSW, usar os handlers existentes, mockar cryptoService, e estruturar com describe/it/expect do Vitest.

Casos de teste obrigatórios:

searchUsersByEmail — retorna array vazio para query com menos de 3 caracteres, sem chamada de rede (client-side short-circuit).
searchUsersByEmail — retorna resultados para query válida (≥3 caracteres), usando o handler MSW de search_users_by_email.
findUserByEmail — retorna objeto {id, email} quando o handler MSW de profiles retorna um match.
findUserByEmail — retorna null quando nenhum match é encontrado.
shareDiagramWithUser — insere share sem erro usando o handler MSW de POST diagram_shares.
shareDiagramWithUser — lança erro com mensagem em português para duplicata (simular resposta com code 23505).
listDiagramShares — retorna lista enriquecida com emails usando handlers de GET diagram_shares e GET profiles.
revokeShare — executa delete sem erro usando handler de DELETE diagram_shares.
loadSharedWithMe — retorna diagramas descriptografados e validados. Mockar decryptDiagramData para retornar os inputs inalterados (mesmo padrão de diagramService.test.ts).
Arquivo src/services/workspaceService.test.ts:

Casos de teste obrigatórios:

getMyWorkspace — retorna WorkspaceRecord mapeado corretamente a partir da resposta do handler MSW de get_user_workspace.
getMyWorkspace — retorna null quando o RPC retorna array vazio.
getWorkspaceMembers — retorna array de WorkspaceMember[] mapeado corretamente usando handler de get_workspace_members.
getWorkspaceMembers — lança erro quando o RPC retorna erro.
removeWorkspaceMember — com userId preenchido, executa delete em workspace_members.
removeWorkspaceMember — com userId nulo, executa delete em workspace_invites.
updateMemberRole — executa update no role usando handler MSW de PATCH workspace_members.
renameWorkspace — executa update no name usando handler MSW de PATCH workspaces.
loadWorkspaceDiagrams — retorna array de diagramas do workspace, excluindo soft-deleted.
Estrutura de cada arquivo
Copyimport { describe, it, expect, vi, beforeAll, afterAll, afterEach } from 'vitest';
import { setupServer } from 'msw/node';
import { handlers } from '../test/handlers'; // ajustar caminho conforme localização real

const server = setupServer(...handlers);

beforeAll(() => server.listen({ onUnhandledRequest: 'error' }));
afterEach(() => server.resetHandlers());
afterAll(() => server.close());

// Mock do cryptoService (apenas para shareService.test.ts)
vi.mock('@/services/cryptoService', () => ({
  encryptDiagramData: vi.fn(async (nodes, edges) => ({ nodes, edges })),
  decryptDiagramData: vi.fn(async (nodes, edges) => ({ nodes, edges })),
}));

describe('shareService', () => {
  // ... testes
});
Se o arquivo de handlers MSW não estiver no caminho ../test/handlers, ajustar o import para o local correto (verificar a estrutura do projeto — pode estar em __tests__/handlers.ts, test/handlers.ts, ou src/test/handlers.ts).

Para os testes de workspaceService, se for necessário adicionar handlers MSW extras para DELETE workspace_members, DELETE workspace_invites, PATCH workspace_members, PATCH workspaces, e GET diagrams?workspace_id=..., adicioná-los ao arquivo de handlers existente.

Arquivos a criar
src/services/shareService.test.ts src/services/workspaceService.test.ts

Arquivos a editar (se necessário)
Arquivo de handlers MSW — adicionar handlers extras para endpoints de workspace que ainda não existam (delete, patch, get diagrams por workspace).

Critério de aceite
Ambos os arquivos passam com vitest run. Cada arquivo testa todas as funções exportadas do respectivo service. Zero dependência de browser ou rede real (tudo via MSW).

Ordem de execução recomendada
1.° ITEM 3 (testes) — não altera código de produção, menor risco de regressão. 2.° ITEM 1 (CSS PricingCards) — refatoração visual sem impacto funcional. 3.° ITEM 2 (integrityWarning UI) — pequena adição funcional em componente existente.

Regras
Não alterar comportamento funcional existente.
Manter todos os testes passando (vitest run) após cada item.
Editar apenas: src/components/, src/services/, src/pages/, src/i18n/, e arquivo de handlers MSW.
Sem dependências de browser (Playwright, Puppeteer, etc.).
Referenciar códigos do PRD nos comentários: // PRD-v3 ITEM-1, // PRD-v3 ITEM-2, // PRD-v3 ITEM-3.
Atualizar chaves i18n em ambos os locales (pt + en) se necessário.