PRD — Remediação de Riscos de Auditoria Técnica
MicroFlow Architect — Score 85 → 92+
Versão: 1.0 Data: 28/03/2026 Objetivo: Sanar todos os riscos identificados na auditoria técnica de 28/03/2026 sem alterar funcionalidades existentes, elevando o score de saúde de 85 para 92+.

Contexto para a IA: Este é um projeto React 18 + TypeScript + Vite + Zustand + Supabase. Todas as alterações devem manter backward compatibility total. Não criar novos arquivos de teste. Não alterar lógica de negócio. Cada item é uma correção cirúrgica em arquivos existentes.

ITEM 1 — Habilitar strictNullChecks no TypeScript
Arquivo: tsconfig.app.json

O que fazer: Alterar strictNullChecks para true e noImplicitAny para true no compilerOptions. Em seguida, corrigir todos os erros de compilação resultantes em todo o projeto. Os erros mais comuns serão variáveis possivelmente null ou undefined que precisam de null-checks (operador ?., ?? defaultValue, ou type narrowing com if). Não usar as any nem @ts-ignore para suprimir erros. Cada correção deve ser mínima e preservar a lógica existente.

Arquivo adicional: tsconfig.json — alterar strictNullChecks para true e noImplicitAny para true também neste arquivo raiz.

Critério de aceite: npm run build compila sem erros com as novas flags habilitadas.

ITEM 2 — Remover credenciais hardcoded do vite.config.ts
Arquivo: vite.config.ts

O que fazer: Remover as constantes fallbackSupabaseUrl e fallbackSupabasePublishableKey completamente. Alterar o bloco define para ler exclusivamente de variáveis de ambiente, lançando erro claro no build se ausentes. O novo bloco define deve ficar assim:

Copydefine: {
  "import.meta.env.VITE_SUPABASE_URL": JSON.stringify(
    process.env.VITE_SUPABASE_URL || (() => { throw new Error("VITE_SUPABASE_URL is required") })()
  ),
  "import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY": JSON.stringify(
    process.env.VITE_SUPABASE_PUBLISHABLE_KEY || (() => { throw new Error("VITE_SUPABASE_PUBLISHABLE_KEY is required") })()
  ),
},
Critério de aceite: Build falha com mensagem clara se variáveis de ambiente não estiverem configuradas. Nenhuma chave ou URL do Supabase permanece hardcoded no código-fonte.

ITEM 3 — Remover console.logs de debug da Edge Function diagram-crypto
Arquivo: supabase/functions/diagram-crypto/index.ts

O que fazer: Remover todas as linhas de console.log que expõem informações de configuração. Especificamente, remover estas linhas:

Copyconsole.log("Auth Header present:", !!authHeader);
console.log("SUPABASE_URL configured:", !!SUPABASE_URL);
console.log("SUPABASE_ANON_KEY configured:", !!SUPABASE_ANON_KEY);
console.log("Authenticated user:", userData.user.id);
Manter apenas os console.error que logam erros reais (autenticação falhada, action inválida, erro de crypto), pois esses são necessários para troubleshooting.

Critério de aceite: Nenhum console.log existe no arquivo. Apenas console.error permanece para cenários de erro.

ITEM 4 — Corrigir política RLS permissiva na SELECT de diagrams
Arquivo: supabase/all_migrations.sql — adicionar novo bloco SQL ao final do arquivo.

Novo arquivo de migração: Criar supabase/migrations/20260328000001_fix_select_policy_share_token.sql

O que fazer: A política diagrams_select_policy atual (introduzida pela migração de workspaces saas0003) contém OR share_token IS NOT NULL, o que permite que qualquer usuário autenticado leia qualquer diagrama que possua um share_token, mesmo sem ser o dono ou colaborador. O acesso via share_token já é tratado exclusivamente pela function SECURITY DEFINER get_diagram_by_share_token, que bypassa RLS. A cláusula OR share_token IS NOT NULL deve ser removida.

SQL da migração:

Copy-- Fix: Remove permissive share_token clause from diagrams SELECT policy.
-- Access via share_token is handled exclusively by the SECURITY DEFINER
-- function get_diagram_by_share_token(), which bypasses RLS.
-- Keeping share_token IS NOT NULL in the RLS policy allows ANY authenticated
-- user to read ANY diagram that has ever been shared, which is a data leak.

DROP POLICY IF EXISTS "diagrams_select_policy" ON public.diagrams;

CREATE POLICY "diagrams_select_policy"
  ON public.diagrams FOR SELECT
  TO authenticated
  USING (
    deleted_at IS NULL AND (
      owner_id = auth.uid()
      OR EXISTS (
        SELECT 1 FROM public.diagram_shares ds
        WHERE ds.diagram_id = id AND ds.shared_with_id = auth.uid()
      )
      OR (
        workspace_id IS NOT NULL AND EXISTS (
          SELECT 1 FROM public.workspace_members wm
          WHERE wm.workspace_id = diagrams.workspace_id
            AND wm.user_id = auth.uid()
            AND wm.accepted_at IS NOT NULL
        )
      )
    )
  );
Adicionar o mesmo bloco SQL ao final de supabase/all_migrations.sql.

Critério de aceite: A política SELECT de diagrams não contém mais share_token IS NOT NULL. Diagramas compartilhados continuam acessíveis via get_diagram_by_share_token. Usuários autenticados só veem seus próprios diagramas, diagramas compartilhados diretamente com eles, e diagramas de workspaces dos quais são membros.

ITEM 5 — Extrair hooks do DiagramCanvas para reduzir complexidade
Arquivo atual: src/components/DiagramCanvas.tsx

Novos arquivos a criar:

src/hooks/useKeyboardShortcuts.ts
src/hooks/useThemeToggle.ts
src/hooks/useInteractionMode.ts
src/hooks/useDiagramRefresh.ts
src/hooks/useAddNodeToCanvas.ts
O que fazer:

Hook 1 — useKeyboardShortcuts.ts: Extrair toda a lógica do handleKeyDown do DiagramCanvas. O hook recebe como parâmetros as funções deleteSelected, undo, redo, saveRef, openShortcuts (callback para abrir modal de atalhos), clearSelectedNode e clearContextMenu (callbacks para limpar overlays). Retorna o handler handleKeyDown pronto para uso como onKeyDown.

Copyimport { useCallback } from 'react';

interface UseKeyboardShortcutsParams {
  deleteSelected: () => void;
  undo: () => void;
  redo: () => void;
  saveRef: React.RefObject<() => void>;
  openShortcuts: () => void;
  clearSelectedNode: () => void;
  clearContextMenu: () => void;
}

export function useKeyboardShortcuts({
  deleteSelected,
  undo,
  redo,
  saveRef,
  openShortcuts,
  clearSelectedNode,
  clearContextMenu,
}: UseKeyboardShortcutsParams) {
  const handleKeyDown = useCallback((e: React.KeyboardEvent) => {
    if (e.key === 'Delete') { deleteSelected(); clearSelectedNode(); }
    if (e.ctrlKey && e.key === 'z') { e.preventDefault(); undo(); }
    if (e.ctrlKey && e.key === 'y') { e.preventDefault(); redo(); }
    if (e.ctrlKey && e.shiftKey && e.key === 'Z') { e.preventDefault(); redo(); }
    if (e.ctrlKey && e.key === 's') { e.preventDefault(); saveRef.current(); }
    if (e.key === '?') openShortcuts();
    if (e.key === 'Escape') { clearSelectedNode(); clearContextMenu(); }
  }, [deleteSelected, undo, redo, saveRef, openShortcuts, clearSelectedNode, clearContextMenu]);

  return handleKeyDown;
}
Copy
Hook 2 — useThemeToggle.ts: Extrair o estado darkMode, a inicialização a partir de localStorage / prefers-color-scheme, e o callback toggleDarkMode.

Copyimport { useState, useCallback, useEffect } from 'react';

export function useThemeToggle() {
  const [darkMode, setDarkMode] = useState(() => {
    const saved = localStorage.getItem('microflow_theme');
    if (saved !== null) return saved === 'dark';
    return window.matchMedia('(prefers-color-scheme: dark)').matches;
  });

  useEffect(() => {
    document.documentElement.classList.toggle('dark', darkMode);
  }, []);

  const toggleDarkMode = useCallback(() => {
    setDarkMode((prev) => {
      const next = !prev;
      document.documentElement.classList.toggle('dark', next);
      localStorage.setItem('microflow_theme', next ? 'dark' : 'light');
      return next;
    });
  }, []);

  return { darkMode, toggleDarkMode };
}
Hook 3 — useInteractionMode.ts: Extrair o estado interactionMode e o callback handleSetInteractionMode.

Copyimport { useState, useCallback } from 'react';

export type InteractionMode = 'pan' | 'select';

export function useInteractionMode() {
  const [interactionMode, setInteractionMode] = useState<InteractionMode>(
    () => localStorage.getItem('microflow_interaction_mode') === 'select' ? 'select' : 'pan',
  );

  const handleSetInteractionMode = useCallback((mode: InteractionMode) => {
    setInteractionMode(mode);
    localStorage.setItem('microflow_interaction_mode', mode);
  }, []);

  return { interactionMode, setInteractionMode: handleSetInteractionMode };
}
Hook 4 — useDiagramRefresh.ts: Extrair refreshing, lastLoadedUpdatedAtRef e handleRefreshDiagram.

Copyimport { useState, useCallback, useRef } from 'react';
import { useTranslation } from 'react-i18next';
import { useDiagramStore } from '@/store/diagramStore';
import { loadDiagramById } from '@/services/diagramService';
import { toast } from '@/hooks/use-toast';

export function useDiagramRefresh() {
  const { t } = useTranslation();
  const [refreshing, setRefreshing] = useState(false);
  const lastLoadedUpdatedAtRef = useRef<string | null>(null);

  const handleRefreshDiagram = useCallback(async () => {
    const { currentDiagramId: diagramId, diagramName } = useDiagramStore.getState();
    const loadDiagram = useDiagramStore.getState().loadDiagram;
    const setDiagramName = useDiagramStore.getState().setDiagramName;

    if (!diagramId) { toast({ title: t('canvas.saveFirst') }); return; }
    setRefreshing(true);
    try {
      const record = await loadDiagramById(diagramId);
      if (!record) { toast({ title: t('canvas.notFound'), variant: 'destructive' }); return; }
      if (record.updated_at === lastLoadedUpdatedAtRef.current) {
        toast({ title: t('canvas.alreadyUpdated') });
      } else {
        const temporal = useDiagramStore.temporal.getState();
        temporal.pause();
        loadDiagram(record.nodes, record.edges);
        if (record.title && record.title !== diagramName) setDiagramName(record.title);
        temporal.resume();
        lastLoadedUpdatedAtRef.current = record.updated_at;
        toast({ title: t('canvas.updateSuccess') });
      }
    } catch { toast({ title: t('canvas.updateError'), variant: 'destructive' }); }
    finally { setRefreshing(false); }
  }, [t]);

  return { refreshing, handleRefreshDiagram, lastLoadedUpdatedAtRef };
}
Copy
Hook 5 — useAddNodeToCanvas.ts: Extrair handleAddNode que usa screenToFlowPosition e reactFlowWrapper.

Copyimport { useCallback, type RefObject } from 'react';
import { useReactFlow } from '@xyflow/react';
import type { NodeType } from '@/types/diagram';

export function useAddNodeToCanvas(
  addNode: (type: NodeType, subType?: string, position?: { x: number; y: number }) => void,
  wrapperRef: RefObject<HTMLDivElement | null>,
) {
  const { screenToFlowPosition } = useReactFlow();

  const handleAddNode = useCallback((type: NodeType, subType?: string) => {
    const wrapper = wrapperRef.current;
    if (wrapper) {
      const rect = wrapper.getBoundingClientRect();
      const pos = screenToFlowPosition({
        x: rect.left + rect.width / 2 + (Math.random() - 0.5) * 80,
        y: rect.top + rect.height / 2 + (Math.random() - 0.5) * 80,
      });
      addNode(type, subType, pos);
    } else {
      addNode(type, subType);
    }
  }, [screenToFlowPosition, addNode, wrapperRef]);

  return handleAddNode;
}
Alteração no DiagramCanvas.tsx: Após criar os 5 hooks, refatorar o DiagramCanvasInner para importar e usar esses hooks, removendo o código extraído. A lógica e comportamento devem permanecer 100% idênticos. Apenas a organização muda.

As linhas removidas do DiagramCanvas são substituídas por:

Copyimport { useKeyboardShortcuts } from '@/hooks/useKeyboardShortcuts';
import { useThemeToggle } from '@/hooks/useThemeToggle';
import { useInteractionMode } from '@/hooks/useInteractionMode';
import { useDiagramRefresh } from '@/hooks/useDiagramRefresh';
import { useAddNodeToCanvas } from '@/hooks/useAddNodeToCanvas';

// Dentro do componente:
const { darkMode, toggleDarkMode } = useThemeToggle();
const { interactionMode, setInteractionMode: handleSetInteractionMode } = useInteractionMode();
const { refreshing, handleRefreshDiagram } = useDiagramRefresh();
const handleAddNode = useAddNodeToCanvas(addNode, reactFlowWrapper);
const handleKeyDown = useKeyboardShortcuts({
  deleteSelected,
  undo,
  redo,
  saveRef: handleSaveToCloudRef,
  openShortcuts: () => modalsRef.current?.openShortcuts(),
  clearSelectedNode: () => overlaysRef.current?.clearSelectedNode(),
  clearContextMenu: () => overlaysRef.current?.clearContextMenu(),
});
Remover do DiagramCanvasInner todo o código que foi extraído para esses hooks: as declarações de estado darkMode/refreshing/interactionMode, os useCallbacks correspondentes (toggleDarkMode, handleSetInteractionMode, handleKeyDown, handleRefreshDiagram, handleAddNode), e o useEffect do dark mode que está no componente. A variável lastLoadedUpdatedAtRef pode ser removida do componente já que o hook useDiagramRefresh a gerencia internamente. A ref reactFlowWrapper permanece no componente porque também é usada no JSX como ref={reactFlowWrapper}.

Critério de aceite: O DiagramCanvas.tsx diminui em pelo menos 80 linhas. Todos os 5 hooks são importados e usados. O comportamento da aplicação permanece idêntico. Build compila sem erros.

ITEM 6 — Otimizar subscriptions do Zustand no DiagramCanvas com useShallow
Arquivo: src/components/DiagramCanvas.tsx

O que fazer: Agrupar os selectors do useDiagramStore que são lidos juntos em um único useShallow selector para reduzir re-renders. Importar useShallow de zustand/react/shallow.

Substituir os selectors individuais de state:

Copy// ANTES (15+ subscriptions individuais):
const nodes = useDiagramStore((s) => s.nodes);
const edges = useDiagramStore((s) => s.edges);
const diagramName = useDiagramStore((s) => s.diagramName);
const diagramId = useDiagramStore((s) => s.currentDiagramId);
const isCollaborator = useDiagramStore((s) => s.isCollaborator);
const setDiagramName = useDiagramStore((s) => s.setDiagramName);
const onNodesChange = useDiagramStore((s) => s.onNodesChange);
const onEdgesChange = useDiagramStore((s) => s.onEdgesChange);
const onConnectAction = useDiagramStore((s) => s.onConnect);
const onNodeDragHandler = useDiagramStore((s) => s.onNodeDragHandler);
const addNode = useDiagramStore((s) => s.addNode);
const addNodesFromSource = useDiagramStore((s) => s.addNodesFromSource);
const deleteSelected = useDiagramStore((s) => s.deleteSelected);
const autoLayout = useDiagramStore((s) => s.autoLayout);
const autoLayoutELK = useDiagramStore((s) => s.autoLayoutELK);
const clearCanvas = useDiagramStore((s) => s.clearCanvas);
const loadDiagram = useDiagramStore((s) => s.loadDiagram);
Por dois grupos lógicos com useShallow:

Copyimport { useShallow } from 'zustand/react/shallow';

// Grupo 1: dados reativos (mudam frequentemente, disparam re-render)
const { nodes, edges, diagramName, currentDiagramId: diagramId, isCollaborator } = useDiagramStore(
  useShallow((s) => ({
    nodes: s.nodes,
    edges: s.edges,
    diagramName: s.diagramName,
    currentDiagramId: s.currentDiagramId,
    isCollaborator: s.isCollaborator,
  }))
);

// Grupo 2: actions (referências estáveis, nunca mudam)
const {
  setDiagramName,
  onNodesChange,
  onEdgesChange,
  onConnect: onConnectAction,
  onNodeDragHandler,
  addNode,
  addNodesFromSource,
  deleteSelected,
  autoLayout,
  autoLayoutELK,
  clearCanvas,
  loadDiagram,
} = useDiagramStore(
  useShallow((s) => ({
    setDiagramName: s.setDiagramName,
    onNodesChange: s.onNodesChange,
    onEdgesChange: s.onEdgesChange,
    onConnect: s.onConnect,
    onNodeDragHandler: s.onNodeDragHandler,
    addNode: s.addNode,
    addNodesFromSource: s.addNodesFromSource,
    deleteSelected: s.deleteSelected,
    autoLayout: s.autoLayout,
    autoLayoutELK: s.autoLayoutELK,
    clearCanvas: s.clearCanvas,
    loadDiagram: s.loadDiagram,
  }))
);
Copy
Critério de aceite: O DiagramCanvas passa de 17 chamadas useDiagramStore() individuais para 2 chamadas agrupadas com useShallow. O comportamento permanece idêntico. Build compila sem erros.

ITEM 7 — Corrigir fallback de CORS na Edge Function share-diagram
Arquivo: supabase/functions/share-diagram/index.ts

O que fazer: Na função getCorsHeaders, quando a origin não é permitida, o fallback é ALLOWED_ORIGINS[0] || "*". O wildcard "*" é inseguro. Alterar para retornar string vazia quando não há origins configuradas, igual ao padrão da Edge Function diagram-crypto.

Alterar esta linha:

Copy"Access-Control-Allow-Origin": allowed ? origin : ALLOWED_ORIGINS[0] || "*",
Para:

Copy"Access-Control-Allow-Origin": allowed ? origin : ALLOWED_ORIGINS[0] || "",
Critério de aceite: O CORS da Edge Function share-diagram nunca retorna * como origin. Comportamento idêntico para origins permitidas.

ITEM 8 — Adicionar campo content_hash para detecção de conflitos
Novo arquivo de migração: Criar supabase/migrations/20260328000002_add_content_hash.sql

Arquivo existente: Adicionar ao final de supabase/all_migrations.sql

O que fazer: Adicionar uma coluna content_hash à tabela diagrams que armazena um hash MD5 do conteúdo serializado (nodes + edges) no momento do save. Isso permite detecção de conflitos em edição colaborativa sem descriptografar o conteúdo.

SQL:

Copy-- Adicionar campo content_hash para detecção de conflitos em edição colaborativa.
-- O hash é calculado no client antes da encriptação e salvo junto ao diagrama.
-- Permite comparar se o conteúdo mudou sem precisar descriptografar.

ALTER TABLE public.diagrams
  ADD COLUMN IF NOT EXISTS content_hash text;

COMMENT ON COLUMN public.diagrams.content_hash IS
  'MD5 do JSON serializado (nodes+edges) antes da encriptação. '
  'Usado para detecção de conflitos em saves concorrentes.';
Arquivo: src/services/diagramService.ts

O que fazer: Na função saveDiagram, antes de chamar encryptDiagramData, calcular o hash MD5 do conteúdo. Usar a Web Crypto API (crypto.subtle.digest) para gerar o hash. Adicionar o content_hash ao updatePayload e ao insertPayload.

Adicionar esta função utilitária no topo do arquivo (após os imports):

Copyasync function computeContentHash(nodes: DiagramNode[], edges: DiagramEdge[]): Promise<string> {
  const content = JSON.stringify({ nodes, edges });
  const encoded = new TextEncoder().encode(content);
  const buffer = await crypto.subtle.digest('SHA-256', encoded);
  const hashArray = Array.from(new Uint8Array(buffer));
  return hashArray.map(b => b.toString(16).padStart(2, '0')).join('');
}
Na função saveDiagram, adicionar antes da encriptação:

Copyconst contentHash = await computeContentHash(nodes, edges);
E incluir content_hash: contentHash tanto no updatePayload quanto no insertPayload.

Fazer o mesmo na função saveSharedDiagram — adicionar cálculo do content_hash e inclui-lo no updatePayload.

Critério de aceite: A coluna content_hash existe na tabela diagrams. Todo save (novo ou update, incluindo shared) calcula e persiste o hash. Build compila sem erros.

ITEM 9 — Adicionar rate limiting por usuário nas Edge Functions
Arquivo: supabase/functions/diagram-crypto/index.ts

O que fazer: Adicionar um rate limiter simples baseado em um Map em memória (per-instance) que limita a 60 chamadas por minuto por usuário. Adicionar após a validação de autenticação e antes da lógica de negócio.

Adicionar no topo do arquivo (fora do handler serve):

Copy// Rate limiting: 60 requests per minute per user (per instance)
const rateLimitMap = new Map<string, { count: number; resetAt: number }>();
const RATE_LIMIT = 60;
const RATE_WINDOW_MS = 60_000;

function checkRateLimit(userId: string): boolean {
  const now = Date.now();
  const entry = rateLimitMap.get(userId);
  if (!entry || now > entry.resetAt) {
    rateLimitMap.set(userId, { count: 1, resetAt: now + RATE_WINDOW_MS });
    return true;
  }
  if (entry.count >= RATE_LIMIT) return false;
  entry.count++;
  return true;
}
Dentro do handler, após console.log("Authenticated user:", ...) (ou onde essa linha estava antes de ser removida pelo ITEM 3) e antes do bloco de business logic, adicionar:

Copyif (!checkRateLimit(userData.user.id)) {
  return new Response(
    JSON.stringify({ error: "Rate limit exceeded. Try again in a minute." }),
    { status: 429, headers: { ...corsHeaders, "Content-Type": "application/json" } },
  );
}
Fazer o mesmo no arquivo: supabase/functions/share-diagram/index.ts — adicionar o mesmo rate limiter com limite de 20 por minuto (compartilhamento é menos frequente).

Critério de aceite: Ambas Edge Functions retornam 429 quando o rate limit é excedido. Usuários normais nunca atingem o limite em uso normal.

ITEM 10 — Garantir consistência entre migrations individuais e all_migrations.sql
Arquivo: supabase/all_migrations.sql

O que fazer: Adicionar um comentário de cabeçalho no início de all_migrations.sql documentando que este é um arquivo consolidado e incluir instruções de manutenção. Adicionar ao topo do arquivo:

Copy-- ============================================================
-- ALL MIGRATIONS (Consolidated)
-- ============================================================
-- Este arquivo é a consolidação sequencial de todas as migrações
-- individuais da pasta supabase/migrations/.
--
-- REGRA DE MANUTENÇÃO:
-- Ao criar uma nova migração em supabase/migrations/,
-- SEMPRE adicionar o conteúdo ao final deste arquivo,
-- separado por um comentário com o nome do arquivo de origem.
--
-- Última atualização: 28/03/2026
-- ============================================================
Além disso, adicionar comentários separadores antes de cada bloco de migração existente no arquivo, identificando qual arquivo de migração originou aquele bloco. Usar o padrão:

Copy-- ── migration: 20260XXX_nome_da_migration.sql ──
Critério de aceite: O arquivo all_migrations.sql tem cabeçalho explicativo e cada bloco de migração está identificado com comentário.

ITEM 11 — Adicionar DOMPurify na renderização de labels de nodes
Arquivo: src/components/nodes/ServiceNode.tsx

O que fazer: O projeto já possui dompurify como dependência mas não há evidência de uso nos nodes que renderizam labels dinâmicas (criadas pelo usuário ou pela IA). Adicionar sanitização com DOMPurify em todos os pontos onde labels de usuário são renderizadas.

Em cada arquivo de node (ServiceNode.tsx, DatabaseNode.tsx, QueueNode.tsx, ExternalNode.tsx), importar DOMPurify e sanitizar o data.label antes de renderizar:

Copyimport DOMPurify from 'dompurify';

// Dentro do componente, antes do return:
const safeLabel = DOMPurify.sanitize(data.label || '', { ALLOWED_TAGS: [] });
Usar safeLabel em vez de data.label em toda a renderização do componente. A opção { ALLOWED_TAGS: [] } garante que nenhuma tag HTML é permitida — apenas texto puro.

Fazer o mesmo para data.subType se ele for renderizado diretamente no JSX.

Fazer o mesmo para itens de data.internalDatabases e data.internalServices no ServiceNode.tsx.

Arquivos afetados:

src/components/nodes/ServiceNode.tsx
src/components/nodes/DatabaseNode.tsx
src/components/nodes/QueueNode.tsx
src/components/nodes/ExternalNode.tsx
Critério de aceite: Todas as labels de nodes renderizadas na UI passam por DOMPurify.sanitize antes da renderização. Nenhum HTML injetado em labels é renderizado como markup.

ITEM 12 — Adicionar índice parcial para diagrams.content_hash
Novo arquivo de migração: Criar supabase/migrations/20260328000003_add_content_hash_index.sql

Arquivo existente: Adicionar ao final de supabase/all_migrations.sql

SQL:

Copy-- Índice para lookups de content_hash (detecção de conflitos)
CREATE INDEX IF NOT EXISTS idx_diagrams_content_hash
  ON public.diagrams (id, content_hash)
  WHERE content_hash IS NOT NULL;
Critério de aceite: O índice é criado. Migração é adicionada tanto no arquivo individual quanto no consolidado.

ITEM 13 — Adicionar safe guards de null nos stores para strictNullChecks
Arquivo: src/store/diagramStore.ts

O que fazer: Com strictNullChecks: true, o TypeScript vai reclamar de pontos onde o tipo pode ser null ou undefined. Especificamente:

No onConnect, o find() pode retornar undefined. Adicionar guards:

Copyconst sourceNode = state.nodes.find((n) => n.id === connection.source);
const targetNode = state.nodes.find((n) => n.id === connection.target);
if (!sourceNode || !targetNode) return { edges: state.edges };
No addNodesFromSource, após const sourceNode = nodes.find(...):

Copyif (!sourceNode) return;
Essa linha já existe — apenas garantir que o TypeScript reconhece que após ela, sourceNode não é undefined.

No addNode, o subType pode ser undefined. O labelMap para database e queue usa subType || 'Oracle' e subType || 'IBM MQ', o que já é safe. Nenhuma alteração necessária aqui.

Arquivo: src/hooks/useAuth.tsx

Com strictNullChecks, o createContext com valor default precisa de um tipo que aceite null. O tipo já está correto (User | null, Session | null). Verificar se a function signOut no default do context precisa de ajuste — o tipo async () => {} retorna Promise<void>, o que é correto.

Arquivo: src/integrations/supabase/client.ts

As variáveis SUPABASE_URL e SUPABASE_PUBLISHABLE_KEY vindas de import.meta.env podem ser undefined com strictNullChecks. Adicionar assertion:

Copyconst SUPABASE_URL = import.meta.env.VITE_SUPABASE_URL;
const SUPABASE_PUBLISHABLE_KEY = import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY;

if (!SUPABASE_URL || !SUPABASE_PUBLISHABLE_KEY) {
  throw new Error('Supabase environment variables are not configured');
}
Critério de aceite: Todos os erros de compilação gerados por strictNullChecks: true são corrigidos sem uso de as any, @ts-ignore, ou ! non-null assertion operator (exceto em pontos onde o valor é comprovadamente não-null pelo fluxo).

Resumo de Arquivos Afetados
#	Arquivo	Ação
1	tsconfig.json	Editar
1	tsconfig.app.json	Editar
2	vite.config.ts	Editar
3	supabase/functions/diagram-crypto/index.ts	Editar
4	supabase/migrations/20260328000001_fix_select_policy_share_token.sql	Criar
4	supabase/all_migrations.sql	Editar
5	src/hooks/useKeyboardShortcuts.ts	Criar
5	src/hooks/useThemeToggle.ts	Criar
5	src/hooks/useInteractionMode.ts	Criar
5	src/hooks/useDiagramRefresh.ts	Criar
5	src/hooks/useAddNodeToCanvas.ts	Criar
5	src/components/DiagramCanvas.tsx	Editar
6	src/components/DiagramCanvas.tsx	Editar
7	supabase/functions/share-diagram/index.ts	Editar
8	supabase/migrations/20260328000002_add_content_hash.sql	Criar
8	src/services/diagramService.ts	Editar
9	supabase/functions/diagram-crypto/index.ts	Editar
9	supabase/functions/share-diagram/index.ts	Editar
10	supabase/all_migrations.sql	Editar
11	src/components/nodes/ServiceNode.tsx	Editar
11	src/components/nodes/DatabaseNode.tsx	Editar
11	src/components/nodes/QueueNode.tsx	Editar
11	src/components/nodes/ExternalNode.tsx	Editar
12	supabase/migrations/20260328000003_add_content_hash_index.sql	Criar
13	src/store/diagramStore.ts	Editar
13	src/integrations/supabase/client.ts	Editar
Ordem de Execução Recomendada
A execução deve seguir esta ordem para evitar erros de compilação intermediários:

Fase 1 (sem dependências entre si): Itens 2, 3, 7, 10, 11 — são edições isoladas que não quebram nada.

Fase 2 (extração de hooks): Item 5 — criar os 5 hooks e refatorar o DiagramCanvas.

Fase 3 (otimização): Item 6 — useShallow no DiagramCanvas (depende do Item 5 estar concluído).

Fase 4 (banco de dados): Itens 4, 8, 12 — migrações SQL e ajustes no diagramService.

Fase 5 (rate limiting): Item 9 — rate limiter nas Edge Functions.

Fase 6 (strictNullChecks — por último): Itens 1 e 13 — habilitar strict mode e corrigir todos os erros de compilação resultantes. Este é o item mais impactante e deve ser feito por último para que as refatorações anteriores já estejam estáveis.

Critério Global de Aceite
Todos os itens estão concluídos quando npm run build compila sem erros, npm run lint não reporta novos warnings, e as migrações SQL podem ser aplicadas sequencialmente sem erro em um banco Supabase limpo.