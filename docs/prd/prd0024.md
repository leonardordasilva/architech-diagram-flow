PRD-0024 — Caminho para 100/100
ITEM 1 — Corrigir regressão share_token IS NOT NULL na SELECT policy
Arquivo: supabase/all_migrations.sql (nova migration 25)
Ação: Criar migration que dropa e recria diagrams_select_policy removendo a cláusula OR share_token IS NOT NULL. O acesso por share_token deve continuar exclusivamente via RPC get_diagram_by_share_token. A policy final deve ser:

CopyCREATE POLICY "diagrams_select_policy"
  ON public.diagrams FOR SELECT TO authenticated
  USING (
    deleted_at IS NULL AND (
      owner_id = auth.uid()
      OR EXISTS (SELECT 1 FROM public.diagram_shares ds WHERE ds.diagram_id = id AND ds.shared_with_id = auth.uid())
      OR (workspace_id IS NOT NULL AND EXISTS (
        SELECT 1 FROM public.workspace_members wm
        WHERE wm.workspace_id = diagrams.workspace_id AND wm.user_id = auth.uid() AND wm.accepted_at IS NOT NULL
      ))
    )
  );
Impacto: Banco +1pt → 20/20

ITEM 2 — Remover unsafe-inline de style-src no CSP
Arquivo: index.html
Ação: Substituir a importação de Google Fonts via <link> por @font-face local (os pacotes @fontsource/space-grotesk e @fontsource/dm-sans já são importados em main.tsx). Remover os <link> de fonts.googleapis.com e as diretivas preconnect. Atualizar CSP para style-src 'self' (sem 'unsafe-inline' e sem https://fonts.googleapis.com). Remover font-src https://fonts.gstatic.com (agora servido localmente).
Impacto: Segurança +1pt → 19/20

ITEM 3 — Testes de integração para hooks Supabase com MSW
Arquivos novos:

src/hooks/useSaveDiagram.test.ts
src/hooks/useRealtimeCollab.test.ts
src/test/mswHandlers.ts (mock service worker handlers para Supabase)
src/test/setup.ts (setup MSW server)
Ação: Usar MSW (Mock Service Worker) para interceptar chamadas Supabase e testar os hooks useSaveDiagram (save → debounce → chama supabase.update, erro → toast) e useRealtimeCollab (broadcast → valida Zod → atualiza store, payload inválido → ignora). Adicionar pelo menos 8 test cases cobrindo happy path, error handling, e edge cases (encrypted payload, concurrent saves, etc.).
Impacto: Crescimento +1pt → 20/20

ITEM 4 — Adicionar ADR-001 (Architecture Decision Records)
Arquivo novo: docs/adr/001-encryption-at-rest.md
Ação: Documentar a decisão de criptografia client→edge-function→DB com AES-256-GCM, incluindo contexto, alternativas avaliadas (Supabase Vault, client-only encryption, column-level encryption), decisão final e consequências. Formato padrão MADR (Markdown ADR).
Impacto: Documentação profissional, consolida o score no topo da faixa enterprise.

Fases de Execução
Fase 1 (paralela): Itens 2 e 4
Fase 2 (após fase 1): Item 1 (requer verificação de que o frontend não depende da policy SELECT via share_token — o código de loadDiagramByToken usa a RPC, confirmado)
Fase 3: Item 3